services:
  - type: web
    name: armoriq
    env: node
    plan: starter
    buildCommand: npm install && cd server && npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
